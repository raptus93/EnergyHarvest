<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/menu_background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ImageView
        android:id="@+id/login_logo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:contentDescription="@string/description_logo_main"
        android:layout_centerHorizontal="true"
        android:src="@drawable/logo" />
    
    <EditText
        android:id="@+id/login_edit_text_email"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:ems="10"
        android:gravity="left"
        android:hint="@string/hint_email"
        android:inputType="textEmailAddress" />
    
   <requestFocus />
   
   <EditText
        android:id="@+id/login_edit_text_password"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/login_edit_text_email"
        android:layout_below="@id/login_edit_text_email"
        android:ems="10"
        android:hint="@string/hint_password"
        android:inputType="textPassword" >
        
    </EditText>
    
   <Button
        android:id="@+id/login_button_registry"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/login_button_login"
        android:layout_alignLeft="@+id/login_edit_text_password"
        android:onClick="goToRegistry"
        android:text="@string/button_register" />

    <Button
        android:id="@+id/login_button_login"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/login_edit_text_password"
        android:layout_below="@+id/login_edit_text_password"
        android:onClick="login"
        android:text="@string/button_login" />

</RelativeLayout>